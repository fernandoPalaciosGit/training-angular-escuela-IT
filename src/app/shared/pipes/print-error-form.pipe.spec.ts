import { PrintErrorFormPipe } from './print-error-form.pipe';

describe('PrintErrorFormPipe', () => {
  it('create an instance', () => {
    const pipe = new PrintErrorFormPipe();
    expect(pipe).toBeTruthy();
  });
});
